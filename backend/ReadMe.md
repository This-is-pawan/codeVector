 database, generate the products yourself. Write a small script that creates 200,000 products, each with a name, category, a price, a unique id, updated_at, created_at time. It’s fine for many products to share the same column values. Commit this generation/seed script to your repo so we can see how you did it. Tip: don’t do a slow approach in a loop.


